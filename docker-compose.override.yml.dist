version: "3.5"
services:

  # nginx web server
  # ------------------------------------------------------------------------------------------------------------------
  webserver:
    ports:
      - "80:80"

  # PHP
  # ------------------------------------------------------------------------------------------------------------------
  php:
    ports:
      - "9000:9000"

  # Postgres
  # ------------------------------------------------------------------------------------------------------------------
  database:
    ports:
      - "3310:3306"

  # Redis
  # ------------------------------------------------------------------------------------------------------------------
  redis:
    ports:
      - "6379:6379"
